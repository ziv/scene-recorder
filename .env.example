# Copy to .env.local and fill in your Cesium ion access token (ion.cesium.com).
# .env.local is used for local development only; the GitHub Pages build reads
# the domain-scoped web token from .env.production instead.
VITE_CESIUM_ION_TOKEN=
